<div class="single-blog">
    <h2>Category</h2>
    <div class="category-list">
        <ul>
            {% for category in categories %}
            <li><a href="#"><i class="fa fa-angle-right"></i>{{ category.name }}<span>({{ category.total }})</span></a></li>
            {% endfor %}
        </ul>
    </div>
</div>